
services:
    server:
        build:
            dockerfile: ./docker/prod/server/Dockerfile
            context: .
        ports:
            - 127.0.0.1:9000:80
    web:
        build:
            dockerfile: ./docker/prod/web/Dockerfile
            context: .
        ports:
            - 127.0.0.1:9001:80
