# PL Sign Server

Pro-Lite sign server written in Python. This repo consists of:

* A simple API server that allows for posting messages
* A simple front-end for posting messages
* A websocket client that pulls messages from the server and updates the (local) sign hardware
